on: [push, pull_request]
name: CI

jobs:
  build:
    strategy:
      max-parallel: 6
      matrix:
        os: [ubuntu-latest, macos-latest]
        name:
          [
            btor,
            cvc4,
            msat,
            yices2,
            z3
          ]

        include:
          - name: msat
            setup_opts: --auto-yes

    name: ${{ matrix.os }}:${{ matrix.name }} 
    runs-on: ${{ matrix.os}}

    steps:
      - uses: actions/checkout@v2

      - name: Install Packages (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cmake \
            libgmp-dev \
            openjdk-8-jdk

      - name: Install Packages (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install \
            gmp

      - name: Setup Underlying Solver
        run: ./travis-scripts/setup-${{ matrix.name }}.sh ${{ matrix.setup_opts }}
