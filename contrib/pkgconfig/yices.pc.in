prefix=${install_dir}
includedir=${prefix}/include
libdir=${prefix}/lib

Name: yices
Version: ${_version}
Description: The Yices SMT Solver
Requires.private: gmp
Cflags: -I${includedir}
Libs: -L${libdir} -lyices
